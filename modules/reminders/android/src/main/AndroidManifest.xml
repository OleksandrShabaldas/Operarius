<manifest xmlns:android="http://schemas.android.com/apk/res/android">

  <!-- Posting reminders (Android 13+ asks the user) -->
  <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
  <!-- On-time alarms: granted automatically on Android 13+ (USE_EXACT_ALARM),
       on 12 / 12L the user-grantable one. -->
  <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" android:maxSdkVersion="32" />
  <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
  <!-- The reminder screen: over the lock screen, and over other apps while in use -->
  <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
  <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
  <!-- Ringing: a foreground service that keeps the alarm alive until dismissed -->
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SYSTEM_EXEMPTED" />
  <uses-permission android:name="android.permission.WAKE_LOCK" />
  <uses-permission android:name="android.permission.VIBRATE" />
  <!-- Surviving reboots, and not being put to sleep by battery optimization -->
  <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
  <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

  <!-- Vendor "auto-start" screens we can send the user to (package visibility) -->
  <queries>
    <package android:name="com.miui.securitycenter" />
    <package android:name="com.huawei.systemmanager" />
    <package android:name="com.hihonor.systemmanager" />
    <package android:name="com.coloros.safecenter" />
    <package android:name="com.oppo.safe" />
    <package android:name="com.vivo.permissionmanager" />
    <package android:name="com.iqoo.secure" />
    <package android:name="com.oneplus.security" />
    <package android:name="com.asus.mobilemanager" />
    <package android:name="com.letv.android.letvsafe" />
    <package android:name="com.samsung.android.lool" />
  </queries>

  <application>
    <activity
      android:name="com.operarius.reminders.AlarmActivity"
      android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|keyboard|keyboardHidden|navigation|uiMode"
      android:excludeFromRecents="true"
      android:exported="false"
      android:launchMode="singleTask"
      android:screenOrientation="portrait"
      android:showWhenLocked="true"
      android:taskAffinity="com.operarius.app.reminder"
      android:theme="@style/OperariusAlarmTheme"
      android:turnScreenOn="true" />

    <activity
      android:name="com.operarius.reminders.OpenTaskActivity"
      android:excludeFromRecents="true"
      android:exported="false"
      android:noHistory="true"
      android:taskAffinity=""
      android:theme="@android:style/Theme.Translucent.NoTitleBar" />

    <service
      android:name="com.operarius.reminders.AlarmService"
      android:exported="false"
      android:foregroundServiceType="systemExempted" />

    <receiver
      android:name="com.operarius.reminders.ReminderReceiver"
      android:exported="false" />

    <receiver
      android:name="com.operarius.reminders.BootReceiver"
      android:exported="true">
      <intent-filter>
        <action android:name="android.intent.action.BOOT_COMPLETED" />
        <action android:name="android.intent.action.QUICKBOOT_POWERON" />
        <action android:name="com.htc.intent.action.QUICKBOOT_POWERON" />
        <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
        <action android:name="android.intent.action.TIME_SET" />
        <action android:name="android.intent.action.TIMEZONE_CHANGED" />
        <action android:name="android.app.action.SCHEDULE_EXACT_ALARM_PERMISSION_STATE_CHANGED" />
      </intent-filter>
    </receiver>
  </application>
</manifest>
